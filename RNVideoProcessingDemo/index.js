/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {VideoProcessing} from './VideoProcessing';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => VideoProcessing);
